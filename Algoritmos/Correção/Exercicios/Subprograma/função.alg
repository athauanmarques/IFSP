algoritmo "subprogramas"
// Função :
// Autor :
// Data : 16/05/2013
// Seção de Declarações 
var

res, b, n: inteiro

procedimento soma(x,y): inteiro var result, inteiro

inicio
x <- x + 1
y <- y + 1
result <- x + y

fimprocedimento

inicio
escreva ("b: ")
leia (b)

escreva ("n: ")
leia (n)

res <- b + n

escreval("resultado é: ", res)

soma (m, b, res)

escreval ("res", res)

fimalgoritmo
