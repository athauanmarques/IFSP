package higthetec;

public class Pessoa {
		//Classe Pessoa
	//Classe é uma representação de um objeto do mundo real
	//Neste caso estamos representando uma pessoa
	/*
	 * Nessa questão dessa classe são apenas atributos, que vão armazenar os dados de cada instancias (pessoa1,pessoa2 e pessoa3),
	 * Da classe TestPessoa
	 * 
	 */
	
	String nome;
	Character sexo;
	/*
	 * atributos são as caracteristicas desse objeto,
	 * Neste caso estamos representado atributos de uma pessoa:
	 * Nome:
	 * Sexo:
	 * poderiamos colocar vários atributos como rg, cpf,  cor do cabelo etc..
	 * 
	 *
	 */
}
